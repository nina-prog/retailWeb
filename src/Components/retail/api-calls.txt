Bisher kam ich nur dazu einen Get Aufruf zu erstellen, welche die Anfrage an folfende URL schickt: http://localhost:8080/hello-world 
und einen String erhalten möchte (HelloWorld Service aus dem Udemy-Kurs)

Die Webiste läuft aktuell auf http://localhost:4200

Folgende API Schnittstellen weden benötigt:

Alle Produkte anzeigen
GET /products

Alle Produkte eines Verkäufers anzeigen
GET /products/{user_name}/products

Löschen eines Produktes
DELETE /products/{user_name}/products/{product_id}

Update eines Produktes
PUT /products/{user_name}/products/{product_id}

Erstellen eines Produktes
POST /products/{user_name}/products


Werden die Filter auf dem Frontend oder Backend angewendet?
Werden die Produkte auf dem Frontend oder Backend sortiert?